package com.sound.lab3_db_chesnokov;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;

import java.util.UUID;

public class RecordFragment extends AppCompatActivity {


/*
    @Override
    public void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_record_fragment);
        UUID crimeId = (UUID) getArguments().getSerializable(ARG_CRIME_ID);
        mRecord = RecordLab.get(getActivity()).getCrime(crimeId);
    }
    @Override
    public void onPause() {
        super.onPause();
        RecordLab.get(getActivity())
                .updateCrime(mRecord);
    }
    */
}
